name: Build BorsaRadar 2.3 APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Rebuild v2.0 base source archive
        run: |
          cat .buildsrc_v20/part00.b64 .buildsrc_v20/part01.b64 .buildsrc_v20/part02.b64 .buildsrc_v20/part03.b64 | base64 -d > BorsaRadar_v2.0_source.zip
          unzip -t BorsaRadar_v2.0_source.zip
      - name: Extract source
        run: |
          rm -rf build-v23
          mkdir build-v23
          unzip -q BorsaRadar_v2.0_source.zip -d build-v23
      - name: Inject full BIST universe
        run: |
          cp app/src/main/java/com/kulanoglu/borsaradar/BistUniverse.java build-v23/BorsaRadar/app/src/main/java/com/kulanoglu/borsaradar/BistUniverse.java
      - name: Apply v2.3 lightweight trader patch
        run: |
          python3 .ci/patch_v23.py
          python3 .ci/fix_v23.py
          python3 .ci/fix_v23b.py
      - name: Build APK
        working-directory: build-v23/BorsaRadar
        run: gradle --no-daemon clean assembleDebug
      - name: Prepare APK
        run: cp build-v23/BorsaRadar/app/build/outputs/apk/debug/app-debug.apk BorsaRadar-v2.3.apk
      - uses: actions/upload-artifact@v4
        with:
          name: BorsaRadar-v2.3-APK
          path: BorsaRadar-v2.3.apk
          if-no-files-found: error
          retention-days: 30
