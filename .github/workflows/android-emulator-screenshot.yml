name: Android Emulator Screenshot
on:
  workflow_dispatch:
  push:
    branches: [ main ]
jobs:
  emulator-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Restore signing key
        run: base64 -d .ci/borsaradar-final.keystore.b64 > .ci/borsaradar-final.keystore
      - name: Build debug APK
        run: |
          gradle --no-daemon assembleDebug --console=plain
          APK=$(find "$GITHUB_WORKSPACE/app/build/outputs" -type f -name "*.apk" | head -1)
          test -n "$APK"
          cp "$APK" "$GITHUB_WORKSPACE/BorsaRadar-emulator.apk"
          ls -lh "$GITHUB_WORKSPACE/BorsaRadar-emulator.apk"
      - name: Boot emulator, install and capture
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          target: google_apis
          arch: x86_64
          profile: pixel_7
          emulator-options: -no-window -gpu swiftshader_indirect -no-snapshot -noaudio -no-boot-anim -camera-back none
          disable-animations: true
          script: |
            mkdir -p emulator-artifacts
            pwd
            ls -lh BorsaRadar-emulator.apk
            adb install -r BorsaRadar-emulator.apk
            adb shell monkey -p com.kulanoglu.borsaradar.finalapp -c android.intent.category.LAUNCHER 1
            sleep 8
            adb exec-out screencap -p > emulator-artifacts/01-home.png
            test -s emulator-artifacts/01-home.png
            file emulator-artifacts/01-home.png
            adb shell am force-stop com.kulanoglu.borsaradar.finalapp
            adb shell am start -n com.kulanoglu.borsaradar.finalapp/com.kulanoglu.borsaradar.ReferenceUiActivity --es screen detail
            sleep 2
            adb exec-out screencap -p > emulator-artifacts/03-detail-general.png
            test -s emulator-artifacts/03-detail-general.png
            adb shell am force-stop com.kulanoglu.borsaradar.finalapp
            adb shell am start -n com.kulanoglu.borsaradar.finalapp/com.kulanoglu.borsaradar.ReferenceUiActivity --es screen chart
            sleep 2
            adb exec-out screencap -p > emulator-artifacts/04-detail-chart.png
            test -s emulator-artifacts/04-detail-chart.png
            adb shell input keyevent 4
            sleep 1
            adb shell input tap 535 2160
            sleep 2
            adb exec-out screencap -p > emulator-artifacts/02-radar.png
            adb shell input tap 805 2160
            sleep 2
            adb exec-out screencap -p > emulator-artifacts/09-portfolio.png
            adb shell input tap 1015 2160
            sleep 2
            adb exec-out screencap -p > emulator-artifacts/11-more.png
            adb shell input tap 500 2050
            sleep 2
            adb exec-out screencap -p > emulator-artifacts/10-strategy.png
            adb shell dumpsys window windows > emulator-artifacts/window.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: BorsaRadar-emulator-screenshots
          path: emulator-artifacts/
