name: BorsaRadar Research Backtest
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'research/**'
      - '.github/workflows/research-backtest.yml'
permissions:
  contents: read
jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run extended walk-forward
        run: PYTHONPATH=research python3 research/backtest_walkforward_long.py
      - name: Show result
        run: cat research/result_walkforward_long.md
      - uses: actions/upload-artifact@v4
        with:
          name: borsaradar-walkforward-long
          path: |
            research/result_walkforward_long.md
            research/result_walkforward_long.json
          if-no-files-found: error
          retention-days: 30
