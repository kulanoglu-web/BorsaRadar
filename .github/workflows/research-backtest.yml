name: BorsaRadar Research Backtest
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'research/**'
      - '.github/workflows/research-backtest.yml'
permissions:
  contents: read
jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run BR-Entry v3
        run: PYTHONPATH=research python3 research/backtest_entry_v3.py
      - name: Show result
        run: cat research/result_entry_v3.md
      - uses: actions/upload-artifact@v4
        with:
          name: borsaradar-entry-v3
          path: |
            research/result_entry_v3.md
            research/result_entry_v3.json
          if-no-files-found: error
          retention-days: 30
