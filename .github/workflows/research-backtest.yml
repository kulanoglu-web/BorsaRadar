name: BorsaRadar Research Backtest
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'research/**'
      - '.github/workflows/research-backtest.yml'
permissions:
  contents: read
jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run BR-SMART
        run: PYTHONPATH=research python3 research/backtest_br_smart.py
      - name: Show result
        run: cat research/result_br_smart.md
      - uses: actions/upload-artifact@v4
        with:
          name: borsaradar-br-smart
          path: |
            research/result_br_smart.md
            research/result_br_smart.json
          if-no-files-found: error
          retention-days: 30
